#include "Tree/tree.h"
#include <iostream>
bool IsSubTree(tree root, tree subRoot);
bool IsMirror(tree root, tree mirror);
int main(){

}


bool IsSubTree(tree root, tree subRoot){


}


bool IsMirror(tree root, tree mirror){

}
